milestone:
  - verify_consumer:
      states:
        pending: startcheck
        approval: checked
        rejected: failed
  - create_ticket:
      states:
        pending: startcheck
        approval: checked
        rejected: failed
  - verify_card:
      states:
        pending: startcheck
        approval: checked
        rejected: failed
  - confirm_ticket:
      states:
        pending: start
        approval: confirm
  - confirm_order:
      states:
        pending: start
        approval: confirm
