package factory

import (
	//"encoding/json"
	"github.com/micro/go-micro/v2/broker"
)

func a(e broker.Event, b broker.Broker)  {
	//m := Message{}
	//err := json.Unmarshal(e.Message().Body,&m)
	//it mustn't ever happens
	//if err != nil{
	//	panic(err)
	//}
	//b.Publish("",e.Message(),nil)

}